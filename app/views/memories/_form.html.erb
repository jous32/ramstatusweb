<%= form_with(model: memory, local: true) do |form| %>
  <% if memory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(memory.errors.count, "error") %> prohibited this memory from being saved:</h2>

      <ul>
      <% memory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :free %>
    <%= form.number_field :free, id: :memory_free %>
  </div>

  <div class="field">
    <%= form.label :used %>
    <%= form.number_field :used, id: :memory_used %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
